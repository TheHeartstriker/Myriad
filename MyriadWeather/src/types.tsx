export type PixelDict = {
  location: { x: number; y: number };
  target: { x: number; y: number };
};

export type PixelImage = {
  r: number;
  g: number;
  b: number;
  a: number;
  x: number;
  y: number;
};
